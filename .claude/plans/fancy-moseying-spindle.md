# prd-to-roadmap 에이전트 정의 수정 계획

## Context

prd-to-roadmap 에이전트를 **프로젝트에 독립적인 범용 에이전트**로 리팩토링합니다. 이 에이전트는 사용자가 PRD 문서를 제공하면, 해당 PRD에서 기술 스택/주제/기능 등 모든 프로젝트 정보를 추출하여 개발 로드맵을 생성합니다. 따라서 에이전트 정의에는 특정 프로젝트 정보가 포함되어서는 안 되고, PRD 분석 방법론과 로드맵 구조 템플릿만 정의되어야 합니다.

추가 요구사항:
1. Phase 2 이전에 "공통 모듈 구성" 단계 추가
2. 각 Phase에 "왜 이 순서인가?" 설명 1~2줄 추가

## 수정 대상

- **파일**: `/Users/yeboong99/Desktop/claude-yeb-coffee/.claude/agents/prd-to-roadmap.md`

## 수정 내용

### 1. 프로젝트 특화 내용 제거

| 섹션 (행 범위) | 변경 |
|----------------|------|
| `description` (frontmatter, 3행) | 예시에서 "캡슐 커피 커뮤니티", "Notion 기반" 등 특정 프로젝트 언급 제거 → 범용 예시로 교체 |
| `## 프로젝트 컨텍스트` (15~25행) | **섹션 전체 삭제** — 프로젝트 정보는 PRD에서 추출 |
| `## 분석 프레임워크` (27~51행) | 39행의 "Notion API, Supabase, Turnstile, Vercel" 제거 → "외부 API, DB, 인증 등 서드파티 서비스" 같은 일반적 표현으로 |
| `## Phase 설계 원칙` (106~116행) | 범용 Phase 구조로 재작성 (아래 상세 참조) |
| `## 태스크 명세 기준` (118~134행) | 예시의 "Notion 클라이언트", "NOTION_API_KEY", "getBrands()" → 범용 예시로 교체 |
| `## 커뮤니케이션 스타일` (148~154행) | "`frontend/` 디렉토리 구조 내" 제거 → "PRD에서 파악한 프로젝트 구조 내" |
| `## 지속적 에이전트 메모리` (174~298행) | Windows 경로 하드코딩 제거 → 현재 프로젝트 기준 상대 경로 사용 안내 |

### 2. "공통 모듈 구성" Phase 추가 + 각 Phase 순서 근거

**Phase 설계 원칙** 섹션을 다음으로 교체:

```markdown
## Phase 설계 원칙

PRD에서 추출한 요구사항을 바탕으로 다음 순서를 기본 골격으로 Phase를 구성하세요. PRD의 실제 내용에 따라 Phase를 조정하거나 병합/분리할 수 있습니다.

1. **Phase 0 - 기반 설정:** 환경 구성, 외부 서비스 계정/키 설정, 데이터베이스 스키마 준비
   > 왜 이 순서인가: 이후 모든 Phase가 환경 변수와 외부 서비스 접근에 의존하므로, 개발 시작 전 인프라를 확보해야 한다.

2. **Phase 1 - 데이터 레이어:** DB 테이블/스키마 생성, 외부 API 연동, 데이터 액세스 함수 구현
   > 왜 이 순서인가: 기능을 구현하려면 데이터를 읽고 쓸 수 있는 기반이 선행되어야 한다.

3. **Phase 2 - 공통 모듈 구성:** 다수의 기능에서 공통으로 호출·참조하는 유틸리티 함수, 타입 변환 매퍼, API 응답 래퍼, 에러 처리 패턴, 공용 훅 등을 사전 정의
   > 왜 이 순서인가: 데이터 레이어가 확보된 후, 핵심 기능 구현 전에 공통 모듈을 준비하면 이후 Phase에서 중복 코드 없이 일관된 패턴으로 개발할 수 있다.

4. **Phase 3 - 핵심 기능:** PRD의 Must Have 기능 구현, placeholder 데이터를 실제 데이터로 교체, CRUD 작업 구현
   > 왜 이 순서인가: 데이터 레이어와 공통 모듈이 준비된 상태에서 핵심 비즈니스 로직에 집중할 수 있다.

5. **Phase 4 - 사용자 경험:** 검색, 필터링, 페이지네이션, 반응형 디자인, 로딩/빈 상태 처리
   > 왜 이 순서인가: 핵심 기능이 작동하는 상태에서 사용성을 개선하는 것이 효율적이며, 기능 완성 후에야 UX 흐름을 정확히 파악할 수 있다.

6. **Phase 5 - 보안 & 품질:** 인증/인가, 스팸 방지, 입력 유효성 검사, 에러 처리 강화
   > 왜 이 순서인가: 모든 기능이 완성된 상태에서 보안 레이어를 추가해야 누락 없이 전체 기능을 커버할 수 있다.

7. **Phase 6 - 성능 & 런칭:** 캐싱, SEO, 배포 최적화, 모니터링 설정
   > 왜 이 순서인가: 기능과 보안이 모두 확보된 최종 단계에서 성능 튜닝과 프로덕션 배포를 진행한다.
```

### 3. PRD 기반 컨텍스트 추출 지침 추가

삭제되는 `## 프로젝트 컨텍스트` 섹션 대신, 에이전트가 PRD를 분석할 때 다음을 추출하도록 지침 추가:
- 프로젝트 주제/목적
- 기술 스택
- 외부 서비스 의존성
- 현재 구현 상태 (있는 경우)
- 기능 요구사항 목록

이 지침은 `## 분석 프레임워크` 섹션의 앞부분에 "PRD 컨텍스트 추출" 단계로 추가합니다.

## 검증 방법

- 수정 후 파일에서 "캡슐", "커피", "Notion", "Supabase", "Turnstile", "Vercel", "pnpm", "Next.js" 등 프로젝트 특화 키워드가 없는지 Grep으로 확인
- Phase 번호가 0~6으로 연속적이고, 각 Phase에 "왜 이 순서인가" 설명이 있는지 확인
- Phase 2가 "공통 모듈 구성"인지 확인
